# Security Policy

WoolGate 重视安全。本项目默认遵循**零遥测、零上报**原则：代码中不包含任何遥测、统计上报或第三方分析 SDK，所有数据只存在于你的本地环境。

## 报告漏洞

**请勿在公开 issue 中提交安全漏洞。**

请通过以下方式私下报告：

- **GitHub Security Advisory**：仓库页面 → Security → Report a vulnerability（推荐）
- **邮件**：通过项目主页的联系方式联系作者（见 README 底部）

请在报告中包含：

1. 影响版本（如 0.5.0）
2. 漏洞类型与严重程度评估
3. 复现步骤（尽量最小化）
4. 影响范围（数据泄露 / 权限问题 / DoS 等）

我们会尽快响应并跟进修复。

## 安全承诺（已内置）

| 项目 | 状态 |
|---|---|
| 遥测/上报 | 无任何遥测代码，数据不出本地 |
| API Key 存储 | Fernet 加密（`ENCRYPTION_KEY`）落库 |
| 默认凭据 | 无默认密码；`GATEWAY_BEARER_TOKEN` / `ENCRYPTION_KEY` 未配置时拒绝启动 |
| 网关鉴权 | 所有 `/v1` 请求校验 Bearer Token |
| 依赖安全 | CI 运行依赖漏洞扫描 + gitleaks 密钥泄漏扫描 |
| 日志 | 请求日志不记录 API Key 明文 |

## 支持版本

| 版本 | 支持状态 |
|---|---|
| 0.5.x | ✅ 当前支持版本（安全修复会优先处理） |
| < 0.5 | ❌ 不再支持，请升级 |

## 部署安全建议

- 仅在内网或受信任网络部署，不要直接暴露公网
- 使用强随机值配置 `GATEWAY_BEARER_TOKEN` 与 `ENCRYPTION_KEY`
- 定期备份 `DATA_PATH` 目录（含加密后的 Key）
- 生产环境建议配合反向代理（TLS 终结）使用
